<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @category.title %>
</p>

<% if can? :update, Brand%>
  <%= link_to 'Edit', edit_category_path(@category) %> |
<% end%>
<%= link_to 'Back', categories_path %>
